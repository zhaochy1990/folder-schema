{
  "name": "folder-schema",
  "version": "0.1.5",
  "description": "generate and validate folder shema",
  "main": "dist/index.js",
  "repository": "https://github.com/zhaochy1990/folder-schema",
  "author": "Zhao Chaoyi",
  "license": "MIT",
  "scripts": {
    "tsc": "tsc",
    "tsc:w": "tsc -w",
    "tsc:r": "tsc -p tsconfig.release.json",
    "clean": "rimraf dist",
    "prepare": "yarn clean && yarn tsc:r"
  },
  "devDependencies": {
    "@types/debug": "^4.1.4",
    "@types/fs-extra": "^5.0.5",
    "rimraf": "^2.6.3",
    "typescript": "^3.4.4"
  },
  "dependencies": {
    "debug": "^4.1.1",
    "fs-extra": "^7.0.1"
  }
}
